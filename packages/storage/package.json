{
  "name": "@wetdrool/storage",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Replaceable content-addressed storage adapters for WetDrool",
  "license": "SEE LICENSE IN LICENSE",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage",
    "lint": "eslint src test",
    "test": "vitest run --exclude 'dist/**' --exclude 'test/**/*.integration.test.ts'",
    "test:integration": "vitest run test/ipfs.integration.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@wetdrool/protocol": "workspace:*",
    "multiformats": "14.0.5"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "vitest": "4.1.10"
  }
}
